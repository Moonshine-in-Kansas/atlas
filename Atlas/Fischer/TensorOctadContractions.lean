import Atlas.Fischer.QuinticMixedRows
import Atlas.Fischer.QuinticOctadReduction

/-! Actual mixed tensor identities and all distinct-octad contractions except
the remaining pure-octad signed network. -/
