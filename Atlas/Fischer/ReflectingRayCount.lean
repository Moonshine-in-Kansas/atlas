import Atlas.Fischer.ReflectingRaySubfamilies
import Atlas.Fischer.ReflectingRootBasicSupport

/-! The actual displayed ray images and their independent exact counts.
The counting audit checks declaration dependencies, excluding the absolute
bound and the subsequent exhaustion argument despite shared imports. -/
